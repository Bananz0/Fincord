<!-- One character cell of the Quick Connect code. Boxed so the code reads as something to copy
     across to another device, rather than as a sentence to skim past. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/secondaryOverlayColor" />
    <corners android:radius="12dp" />
    <stroke
        android:width="1dp"
        android:color="@color/accentColorFainted" />
</shape>
