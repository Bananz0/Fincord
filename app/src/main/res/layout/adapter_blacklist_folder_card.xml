<?xml version="1.0" encoding="utf-8"?>
<!--
    One artist or song. A tick marks the hidden ones rather than a checkbox: the rest of the app
    says "selected" with an accent checkmark, and a Material square here was the only one of its
    kind in the whole app.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <!--
        The clickable view is this one, not the root: it is the one that shows the ripple, and a
        clickable child consumes the touch before the root ever sees it - which is why the row
        looked pressed and did nothing.
    -->
    <LinearLayout
        android:id="@+id/row_content"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center_vertical"
        android:minHeight="54dp"
        android:orientation="horizontal"
        android:paddingHorizontal="14dp"
        android:paddingVertical="6dp">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:fontFamily="@font/inter_regular"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textColor="@color/onSurfaceColor"
                android:textSize="16sp"
                tools:text="A$AP Rocky" />

            <TextView
                android:id="@+id/subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:ellipsize="end"
                android:fontFamily="@font/inter_regular"
                android:includeFontPadding="false"
                android:maxLines="1"
                android:textColor="@color/onSurfaceColorInactive"
                android:textSize="13sp"
                android:visibility="gone"
                tools:text="Kendrick Lamar"
                tools:visibility="visible" />

        </LinearLayout>

        <!-- Fixed width whether or not the tick shows, so titles do not shift as rows are toggled. -->
        <FrameLayout
            android:layout_width="30dp"
            android:layout_height="30dp"
            android:layout_marginStart="8dp">

            <ImageView
                android:id="@+id/checkbox"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:importantForAccessibility="no"
                android:src="@drawable/ic_checkmark"
                android:visibility="gone"
                app:tint="@color/accentColor"
                tools:visibility="visible" />

        </FrameLayout>

    </LinearLayout>

    <View
        android:id="@+id/divider"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:layout_marginStart="14dp"
        android:background="@color/bottomNavigationDividerColor" />

</LinearLayout>
